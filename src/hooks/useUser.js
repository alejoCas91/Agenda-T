export default function useUser() {
  // simulación temporal
  const user = {
    id: "00000000-0000-0000-0000-000000000000",
    role: "admin", // cambiar a "user" para probar
  };

  return user;
}
